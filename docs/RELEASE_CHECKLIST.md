# 四子游戏发布门禁清单

> 状态：当前权威发布清单
> 产品决策确认日期：2026-08-01
> Google Play/Apple 官方要求最后核验日期：2026-08-01
> 国内发行官方要求最后核验日期：2026-08-23
> 使用方式：每个版本从本清单复制一份发布记录，填写 commit、构建环境、测试证据、产物哈希和审批结果。未勾选硬门禁时不得宣称可正式发布。

## 1. 通用门禁

### 1.1 范围与真实性

- [ ] 发布功能严格属于当前阶段，没有提前暴露后续阶段入口。
- [ ] 应用内规则、教程、商店描述、截图和实际代码行为一致。
- [ ] README、路线图、变更日志和发布说明没有无证据的“100%完成”、覆盖率或性能数据。
- [ ] `CHANGELOG.md` 只记录真实发布版本和可复现的验证结果。
- [ ] 已记录本次发布 commit，工作树不包含未知或无关改动。

### 1.2 质量证据

- [ ] `flutter analyze` 无 error；剩余 warning/info 已审阅并记录。
- [ ] Dart/Flutter 单元和 Widget 测试全部通过。
- [ ] 当前阶段集成测试全部通过。
- [ ] 规则契约测试覆盖所有合法、非法、双向吃子和终局优先级。
- [ ] 存档迁移、进程终止恢复和回放一致性测试通过。
- [ ] 无障碍、字号、横竖屏和减少动态效果验证通过。
- [ ] 依赖许可证、字体、图标、音频和图片的商业使用授权可追溯。
- [ ] 发布产物、符号文件和构建日志已归档，产物 SHA-256 已记录。

### 1.3 隐私与诊断

- [ ] Sentry 仅用于匿名崩溃和性能诊断，默认开启，设置中可以关闭。
- [ ] `sendDefaultPii` 等价能力关闭，发送前过滤敏感字段和棋局内容。
- [ ] 不接入行为分析、广告标识、广告 SDK、用户画像或应用内购买。
- [ ] 隐私政策、应用内说明、Google Play Data Safety 和 App Store App Privacy 与最终 SDK 清单一致。
- [ ] 已启用环境的诊断事件使用独立 environment；Phase 1/2 只有 development、production，Phase 3 再加入 staging。
- [ ] Sentry 符号上传凭证和其他密钥未进入仓库或应用资产。

## 2. Phase 1：Android 正式发布

### 2.1 功能门禁

- [ ] 本地双人对战完整。
- [ ] 简单、中等、困难三档 AI 完整。
- [ ] LAN Host/Client、发现、连接、移动、计时、重赛、30 秒重连和全量重同步完整。
- [ ] 单槽自动存档在每次确认落子后写入。
- [ ] 首页显示可用的“继续游戏”。
- [ ] 结局或明确放弃后清除当前存档。
- [ ] 本地双人、AI、LAN 均进入统计。
- [ ] 最近 20 局已完成对局可以逐步回放。
- [ ] 交互式教程和权威规则页面完成。
- [ ] Phase 1 只暴露中文界面和现代东方棋艺主题。
- [ ] 在线、普通对局语音和冥想模式入口未暴露，商店文案也未宣称可用。

### 2.2 Android 工程门禁

- [ ] `minSdk` 明确设置为 26。
- [ ] `compileSdk` 和 `targetSdk` 设置为 36，并在发布构建中复核。
- [ ] Java、Kotlin、Android Gradle Plugin、Gradle 和 Flutter 版本已锁定并记录。
- [ ] Android command-line tools 已安装，SDK licenses 已确认。
- [ ] 最终永久 `applicationId` 已替换开发期 `com.qoder.foursquare`。
- [ ] 正式应用名称和 Android label 已确认。
- [ ] `versionName` 和递增的 `versionCode` 已确认。
- [ ] release 构建不使用 debug key。
- [ ] upload key、app-signing key 和备份责任已确认；密钥文件不进入仓库。
- [ ] 使用正式签名生成 Android App Bundle（AAB）。
- [ ] 使用 bundletool 或 Google Play 交付产物完成安装验证。
- [ ] `RECORD_AUDIO` 等 Phase 4 权限未进入 Phase 1 主清单。
- [ ] LAN 仅声明 `INTERNET` 与组播所需权限；网络不可用或系统阻止本地网络时有可理解的降级体验。
- [ ] Android 26、典型中间版本和 Android 35/36 真机或高可信设备矩阵通过。
- [ ] 冷启动、离线启动、后台、锁屏、升级安装和低存储场景通过。

### 2.3 LAN 专项门禁

- [ ] Host 通过权威绝对截止时间计算回合超时，不依赖后台定时器持续执行。
- [ ] 双方同步首局随机先手，重赛时交替先手。
- [ ] 重复、乱序或缺失消息不会造成静默分叉。
- [ ] 每个状态更新具备可校验的顺序或版本信息。
- [ ] 重连后以 Host 权威快照恢复棋盘、当前方、剩余时间、未吃子计数和历史。
- [ ] Host/Client 互换、家庭 Wi-Fi、手机热点、锁屏和网络切换场景通过。
- [ ] 同一落子横纵双吃能够完整同步，不丢失任一被吃棋子。
- [ ] LAN 不显示或接受撤销、重做请求。

### 2.4 Google Play 发布门禁

- [ ] Google Play 开发者身份已验证。
- [ ] 最终包名已按 Play Console 要求注册。
- [ ] 开发者账户类型和创建日期已确认。
- [ ] 若适用新个人账户规则，至少 12 名测试者连续加入封闭测试 14 天，并保留真实反馈记录。
- [ ] 已完成内部测试。
- [ ] 已完成封闭测试和正式发布资格申请（若适用）。
- [ ] Google Play 预发布报告无未处理的崩溃、ANR 或严重兼容问题。
- [ ] 已填写应用访问权限、内容分级、目标受众、广告声明和 Data Safety。
- [ ] 隐私政策 URL 可公开访问、非 PDF、无地域限制，并可从应用内访问。
- [ ] 商店标题、简短描述、完整描述均只宣传 Phase 1 已交付功能。
- [ ] 512×512 应用图标、1024×500 feature graphic、手机截图和必要的平板截图完成。
- [ ] 截图来自最终 UI，文字与 Phase 1 中文支持范围一致。
- [ ] 发布顺序为内部测试 → 封闭测试 → 正式发布；这些渠道不作为独立 Beta 产品宣传。

### 2.5 Google Play 官方要求核验

以下要求于 2026-08-01 复核，提交前仍需再次查看最新官方政策：

- 2026-08-31 起，新应用和应用更新必须以 Android 16（API 36）或更高版本为目标：[Target API level requirements](https://developer.android.com/google/play/requirements/target-sdk)。
- 新应用在 Google Play 使用 Android App Bundle 发布：[About Android App Bundles](https://developer.android.com/guide/app-bundle)。
- 2023-11-13 后创建的个人开发者账户，在申请正式发布前需满足至少 12 名测试者连续加入封闭测试 14 天等要求：[App testing requirements for new personal developer accounts](https://support.google.com/googleplay/android-developer/answer/14151465)。
- 所有应用都要提供准确的 Data Safety 信息；即使不收集用户数据，也必须完成表单并提供隐私政策：[Provide information for Google Play's Data safety section](https://support.google.com/googleplay/android-developer/answer/10787469)。
- 隐私政策须在 Play Console 和应用内可访问，公开、非地域限制、非 PDF，并说明数据访问、使用、共享、保留和删除：[Google Play Developer Program Policy](https://support.google.com/googleplay/android-developer/answer/17190352)。
- 自 2026-09-30 起，Play 包名须完成 Android 开发者验证和包名注册：[Registering Play package names](https://support.google.com/googleplay/android-developer/answer/16984799)。

## 3. Phase 2：iOS 正式发布

- [ ] `ios/` 工程完整创建并纳入版本管理。
- [ ] 最低系统版本设置为 iOS 13。
- [ ] iOS Bundle ID 与已确认的永久标识一致。
- [ ] Apple Developer Team、证书、Provisioning Profile 和 App Store Connect 权限就绪。
- [ ] 使用符合当期要求的 Xcode 和 SDK 构建；当前门禁为 Xcode 26/iOS 26 SDK 或更高版本。
- [ ] iOS 完整具备 Phase 1 的单机、AI、LAN、存档、统计、最近 20 局回放、规则和教程。
- [ ] `NSLocalNetworkUsageDescription` 和 `_foursquare._tcp` Bonjour 声明准确描述 LAN 用途。
- [ ] Phase 4 前不提前申请麦克风权限。
- [ ] 中文、英文、日文所有用户可见文本均已本地化，Android 同步更新。
- [ ] iOS 13 与当前 iOS、iPhone 与 iPad、横竖屏测试通过。
- [ ] TestFlight 内部和外部验证按发布策略完成。
- [ ] App Privacy、隐私政策、内容分级、App Store 描述、截图和审核备注完成。
- [ ] dSYM 已上传到诊断平台并归档。

Apple 官方要求于 2026-08-01 复核：

- 自 2026-04-28 起，iOS/iPadOS 应用须使用 iOS/iPadOS 26 SDK 或更高版本构建：[Submitting to the App Store](https://developer.apple.com/app-store/submitting/)。
- iOS 应用需要隐私政策 URL，并须声明自身及第三方 SDK 的数据处理实践：[Manage app privacy](https://developer.apple.com/help/app-store-connect/manage-app-information/manage-app-privacy)。

## 4. Phase 3：可靠在线对战发布

### 4.1 环境门禁

- [ ] Phase 1/2 继续只有 development 和 production；staging 仅在 Phase 3 引入。
- [ ] staging 应用、服务器、数据库、域名、密钥和 Sentry environment 与 production 隔离。
- [ ] staging 不读取或复制 production 用户数据。
- [ ] production 仅使用 HTTPS/WSS 和有效证书。
- [ ] 配置启动时进行完整校验，不以示例域名或明文 IP 回退。

### 4.2 客户端与协议门禁

- [ ] 本地随机匿名设备 ID，不要求手机号、邮箱或真实姓名。
- [ ] 服务端绑定 socket、match、设备身份和执棋颜色，客户端字段不能越权。
- [ ] 服务端权威验证全部规则，客户端预测失败时可回滚或重同步。
- [ ] 每个落子都有权威确认或拒绝、序号、协议版本和可校验状态。
- [ ] 多吃子使用列表表示，客户端和服务器不会只保留第一枚被吃棋子。
- [ ] 断线重连、超时竞态、重复提交和全量重同步测试通过。
- [ ] 设置页可删除在线数据并重置身份。
- [ ] 不实现自动跨设备同步。

### 4.3 服务端与运维门禁

- [ ] 使用受支持的 Node.js 版本和锁定依赖的生产镜像。
- [ ] 生产容器运行编译产物，不运行开发热重载进程。
- [ ] 数据库 migration 可前进、回滚并在 staging 演练。
- [ ] 健康检查、优雅停机、限流、输入校验、CORS 白名单和 TLS 完成。
- [ ] 数据库、Redis 不直接暴露到公网。
- [ ] 密钥来自安全配置，不写入 Compose、镜像或仓库。
- [ ] 备份、恢复、保留期和删除任务已验证。
- [ ] Sentry 服务端接入且日志、breadcrumbs、异常上下文完成脱敏。
- [ ] 契约、集成、并发、负载、服务重启和故障注入测试通过。
- [ ] 部署和回滚步骤可由清单复现。

### 4.4 当前本地开发证据（不等于发布完成）

- 服务端权威规则、协议版本、身份/颜色绑定、幂等落子、60 秒超时、30 秒重连、缓存回收和 Socket 输入边界已有本地自动化测试。
- 客户端匿名身份持久化、权威快照/确认/拒绝解析、修订缺口恢复、旧连接隔离，以及“仅在 Socket onConnect 后确认连接成功”的语义已有本地自动化测试。
- 尚未接入生产导航的在线 BLoC/UI 已接入权威状态，中英日基础状态、断线恢复入口、服务端确认前不改棋盘、终局协议完整性和标识隐私已有本地自动化测试。
- 本地真实 Node Socket.IO 双客户端，以及两个具体 Flutter `OnlineGameTransport` 实例直连 Node 生产入口的显式启用型测试，已自动验证匹配、双方收到同一权威提交、断线通知、30 秒内同身份恢复和主动全量快照；这不等同于两台完整 Flutter 应用、真机或 staging 联调已通过。
- 服务端对局记录已使用稳定 `matchId` 和内容哈希幂等写入，终局时固定完整不可变快照；同库持久化 outbox、租约恢复 worker、Prisma migration、生产 migration 容器入口及静态契约均已有本地自动化。真实 PostgreSQL migration/回滚、双 worker 竞争、慢任务租约、备份恢复、在线删除和并发集成仍未验证。
- 生产服务代码已具备 fail-closed 数据库/CORS/可信代理配置、HTTP 与 Engine.IO 限流、liveness/readiness single-flight、输入上限和总时限优雅停机。可信代理网络拓扑、镜像 digest、TLS/WSS、网络级防护和外部部署仍需在 staging 核验。
- 在线入口继续隐藏；staging、两台完整客户端或真机、真实 PostgreSQL、负载、进程重启故障注入、TLS、备份恢复和外部运维门禁仍未完成，因此 Phase 3 不能标记为可用或可发布。终局成功进入 outbox 后可在重启后恢复；数据库从终局发生至进程退出始终不可用时仍没有不丢局保证，关机会返回脱敏失败并保留为 PC-20260801-009 门禁。

## 5. Phase 4：语音与冥想发布

- [ ] 普通对局语音控制和播报完整。
- [ ] 中文无屏冥想模式可以不看屏幕完成整局。
- [ ] 麦克风权限在用户主动进入语音功能后按需请求。
- [ ] 权限用途文案、拒绝路径和设置跳转清晰。
- [ ] 识别失败、TTS 失败、音频焦点中断、来电和蓝牙耳机场景通过。
- [ ] 语音输入不会进入 Sentry 或普通日志。
- [ ] 是否经过系统或第三方语音服务传输已核实并写入隐私政策及商店声明。
- [ ] Phase 4 首期只承诺中文语音，不宣称多语言语音。

### 5.1 当前本地开发证据（不等于 Phase 4 可用）

- 语音入口隐藏期间默认和旧配置回退均关闭语音，已存储的原型开启值不能初始化 TTS 或触发占位播报。
- 语音服务已移除识别/播报原文打印，携带识别内容的对象字符串由自动化测试约束为脱敏摘要。
- 中文坐标已统一使用用户可见的一至四，并对全部 16 格验证与内部 0–3 坐标可逆；4×4 的歧义中心和冲突描述不会自动执行。
- 可替换权限/识别/播报端口、支持中断恢复与播报失败重播的单次监听控制器、严格类型化指令和普通对局 GameEvent 适配器已完成纯核心自动化；隐藏冥想页面已注入该能力，播报失败重播不会重复执行已提交指令。
- 权威冥想 session 核心已覆盖开场后计时、执色/先手、AI 注入、历史、未吃计数、双吃、暂停恢复和 60 秒超时优先；intent handler 覆盖查询、AI 失败重试与退出确认。假端口已完成 15 ply 自然终局的整局无屏闭环，版本化冥想存档已覆盖严格解码、剩余时间冻结、权威恢复和独立 Hive 适配。运行时已覆盖自动保存、绝对时限驱动、AI 耐久化屏障、恢复单飞、销毁后异步隔离、归档失败保留存档和终局条件删除，`StorageService` 已负责冥想 Box 的初始化、重置和关闭。
- 现代东方隐藏页面已具备中英日用途说明、显式语音启用、TTS 完成后才启动开局时钟、单次聆听、暂停恢复、后台超时终局刷新、两段式弃局确认和窄屏可滚动布局。隐藏组合页会在挂载后异步创建或恢复运行时，成功后才构造生产语音 Adapter；加载、无存档、脱敏失败、重试和离页销毁已有 Widget 契约。
- 基于当前锁定依赖的候选生产语音 Adapter 已完成懒加载与插件隔离：权限状态 fail-closed，STT 采用单次中文短指令并隔离迟到回调，TTS 只在真实 completion 后成功，cancel/error 可恢复重播，终止回调超时会熔断。入口仍未进入 Home 或正式导航，也没有新增平台权限；平台权限和设置跳转、真机音频与最终语音引擎隐私外部门禁仍未完成，当前不对用户开放。
- 普通对局已增加不被 Home 使用的隐藏 PVE 语音构造：只有权威状态给出真实 `humanPlayer`、处于玩家回合且用户点击用途说明后才创建 session；位置、完整移动和取消选择只进入既有 GameBloc 规则链，完整移动固定 `isAIMove=false`。完整移动只有在权威状态提交匹配的 move count、起止位置和执色后才播报移动、完整吃子数或终局结果；未提交命令保持静默，AI 状态不截断 TTS，失败/后台中断可重播且不会重复派发命令，新局会清除上一局待播结果。PVP 和普通对局控制动作仍未完成，入口继续隐藏，不能标记为“普通对局语音完整”。

## 6. 国内 Android 应用商店

### 6.1 当前阶段与范围

- [ ] Phase 1 不同步发布国内商店，也不提前接入商店专属 SDK。
- [ ] Google Play 正式版稳定后，再确定目标商店和发布顺序。
- [ ] 首次 Google Play 发布前确认 app-signing key 的跨商店复用和备份方案，避免后续同包名更新发生签名冲突。
- [ ] 国内发布项目单独立项，明确境内发行版本、功能范围、地区、时间和责任人；Google Play 产品范围和无广告/IAP、无行为分析边界保持不变。
- [ ] 已由主体所在地通信管理局、接入服务商、合作出版单位、省级出版管理部门、属地公安机关和目标商店复核适用要求，复核日期与联系人/工单已归档。

### 6.2 主体、名称、权利和网络资源

- [ ] APP 主办者、著作权人、出版单位、运营单位、商店开发者账号主体及授权链已确定并可证明。
- [ ] 正式中文名称、APP 备案名称、软件著作权名称、出版审批名称和商店名称已经一致性核对。
- [ ] 永久包名、签名证书、公钥/MD5、图标、域名、服务器区域、IP 和接入服务商已经锁定。
- [ ] 软件著作权、素材授权、商标/名称检索和运营授权材料齐备。
- [ ] 目标商店账号已实名，账号所有权、操作权限和离职/交接机制已记录。

### 6.3 经营许可与出版前置资质

- [ ] 主体所在地通信管理局、接入服务商和合作出版单位已书面确认经营性 ICP 许可、非经营性网站备案及出版申报的适用关系。
- [ ] 如出版申报或实际服务形态要求经营性 ICP 许可，运营单位已先取得对应增值电信业务经营许可证，并核对许可业务与服务区域。
- [ ] 如相关许可不适用，主管部门、接入服务商或合作出版单位的确认依据已归档，未由项目自行判定豁免。

### 6.4 网络游戏出版审批与实名防沉迷

- [ ] 合作出版单位已确认本项目的国产移动游戏/网络下载单机游戏申报类别及当期材料要求。
- [ ] 出版审批/版号及实名防沉迷如适用，以下对应门禁全部完成；如不适用，出版单位、省级出版管理部门或目标商店的书面确认依据已归档。
- [ ] 如适用，具有网络游戏出版范围的出版单位、符合条件的著作权人和持有 ICP 证的运营单位已经落实。
- [ ] 如适用，出版申报材料齐备：申请文件/表、审读报告、著作权证明、运营合同与资质、截图、中文脚本/屏蔽词库、防沉迷说明、审查账号、安装包和演示视频。
- [ ] 如适用，已取得网络游戏审批批复和出版物号；审批名称、出版/运营单位、平台和申报版本与发布版本一致。
- [ ] 如适用，游戏名称、出版/运营单位、申报平台或内容发生变化时，已由出版单位确认并完成所需变更审批。
- [ ] 如适用，已接入国家新闻出版署网络游戏防沉迷实名认证系统，所有用户实名注册/登录，游客模式无法绕过。
- [ ] 如适用，未成年人服务时段在客户端与服务端均受权威控制，离线、改时钟、断网、重装和多设备场景无法绕过。
- [ ] 当前无充值入口；未成年人付费检查记录为不适用。任何商业化变化需重新通过未成年人保护和支付合规门禁。

### 6.5 网站与 APP 备案

- [ ] 中国大陆托管的网站/隐私页面/API 已按服务形态完成非经营性网站备案判断，并与已取得的经营许可、出版前置审批保持一致。
- [ ] 如适用，非经营性网站备案材料包含主体与负责人、网站/域名、服务项目、接入信息和前置审批文件。
- [ ] APP 已通过网络接入服务商或符合条件的分发平台向主办者住所所在地通信管理局完成备案。
- [ ] APP 备案信息中的名称、图标、域名、包名/ID、公钥/MD5、平台、类目、语种、SDK、接入和前置审批材料与最终包一致。
- [ ] APP 内显著位置已展示备案编号并链接工信部备案系统；网站已按要求展示 ICP 备案号/许可证号。
- [ ] 备案/许可变更、接入变更、主体迁移和停止服务的操作人、时限、注销步骤及证据位置已记录。

### 6.6 个人信息、权限与 SDK

- [ ] `PRIVACY_DATA_MAP.md` 已按国内最终数据流、实名防沉迷、渠道 SDK、远程服务、字段、处理区域和保留期更新，并完成发布前变更审查。
- [ ] 最终安装包已完成静态和动态隐私检测，权限、域名、SDK 和实际网络请求清单已归档。
- [ ] 隐私政策、首次提示、个人信息清单、第三方共享清单、SDK 清单、权限用途、删除/注销入口和商店隐私表一致。
- [ ] SDK 名称、开发者、版本、功能、处理字段、目的、传输对象、区域和保留期限已公开并与备案一致。
- [ ] 每个 SDK 已完成个人信息保护能力和合同责任评估；渠道 SDK 未启用广告、归因、行为分析或用户画像能力。
- [ ] 权限按功能触发申请，拒绝非基本功能权限不影响基本功能；麦克风等 Phase 4 权限不会提前进入国内 Phase 1 正式包。
- [ ] 涉及实名身份或不满十四周岁用户信息时，敏感个人信息和儿童个人信息的必要性、告知同意、访问控制、保留和删除措施已由合规责任人复核。

### 6.7 商店提交与上架验证

- [ ] 每个目标商店使用与实际功能一致的独立素材、年龄分级、隐私和合规声明。
- [ ] 每个商店当期要求的软件著作权、APP 备案、版号/出版审批、营业执照、ICP 证和授权材料已上传。
- [ ] 正式签名包通过商店安全、兼容、隐私和内容检测；驳回原因、修复版本和复核结果已归档。
- [ ] 商店测试/灰度渠道完成下载安装、签名、覆盖升级、数据迁移、LAN/网络、隐私入口和回滚验证。
- [ ] 包体、版本号、签名证书、SHA-256、素材、审核记录、商店公示页面和发布日期已归档。

### 6.8 上线后公安备案与持续义务

- [ ] 正式联网后已在法定时限内向属地公安机关确认并办理适用的公安联网备案。
- [ ] 网站按属地要求在首页底部展示公安联网备案号、图标和查询链接。
- [ ] 主体、负责人、域名、接入、服务器、包名/签名、SDK、隐私数据流、出版/运营单位和服务范围变更均先完成合规影响评估。
- [ ] 下架、停止境内服务或主体迁移时，已完成商店下架及网站/APP/公安备案、许可和出版相关的变更或注销手续。
- [ ] 每季度复核公开备案信息、资格有效期、目标商店政策、SDK 规则和隐私页面准确性。

国内流程与官方来源详见 [POST_LAUNCH_OPERATIONS_PLAN.md](POST_LAUNCH_OPERATIONS_PLAN.md#44-国内-android-商店)。正式办理材料和顺序以当期受理机关、接入服务商、合作出版单位和目标商店复核结果为准。

## 7. 发布前由项目方补齐

### Phase 1 硬门禁

- [ ] 最终应用名称。
- [ ] 永久 Android applicationId。
- [ ] 开发者展示名称或法律主体。
- [ ] 隐私政策公开 URL。
- [ ] 隐私联系和支持邮箱。
- [ ] Sentry Flutter DSN、组织和项目。
- [ ] Google Play 账户类型、创建日期、验证和包名注册状态。
- [ ] 签名密钥所有权、备份和操作责任。
- [ ] 发布国家/地区、目标年龄和内容分级答案。
- [ ] 最终商店素材和发布说明。

### Phase 2 硬门禁

- [ ] iOS Bundle ID。
- [ ] Apple Developer Team 和 App Store Connect 权限。
- [ ] SKU、发布地区和审核联系信息。

### Phase 3 硬门禁

- [ ] staging/production 域名和部署区域。
- [ ] 数据库、缓存和备份方案。
- [ ] 在线数据保留期限和删除 SLA。
- [ ] 服务端 Sentry DSN 和告警接收方。

### Phase 4 硬门禁

- [ ] 最终语音识别与播报引擎及平台支持范围。
- [ ] 语音联网/离线边界、第三方处理区域和数据保留条款。
- [ ] 所需外部账号、凭据提供方式及隐私声明责任。

### 国内 Android 商店硬门禁

- [ ] 中国大陆 APP 主办者、著作权人、出版单位、运营单位和授权链。
- [ ] 目标国内商店、发行地区、开发者账号主体和操作责任人。
- [ ] 正式中文名称、软件著作权、国内包名/签名复用方案和素材权属。
- [ ] 中国大陆域名、服务器区域、网络接入服务商及网站 ICP/经营性 ICP 责任。
- [ ] APP 备案、网络游戏出版审批/版号、公安联网备案的办理责任和完成证明。
- [ ] 实名防沉迷系统接入主体、测试/生产权限和未成年人保护责任人。
- [ ] 国内隐私政策 URL、个人信息负责人、SDK 合规复核人和用户请求渠道。

## 8. 单次发布记录模板

- 版本：
- 阶段/平台：
- Git commit：
- 构建环境：
- `versionName` / `versionCode` 或 iOS build：
- 测试命令及结果：
- 真机矩阵：
- 已知非阻塞问题：
- 产物路径及 SHA-256：
- 符号文件归档位置：
- 隐私声明复核人：
- 发布审批人：
- 渠道和发布时间：
- 回滚版本与步骤：
